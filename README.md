# Academic Resume PDF Editor

这是一个独立的静态网页项目，不会修改上级目录里的原页面文件。

## 使用

直接打开 `index.html`，或在本目录启动任意静态服务器。

- 简历固定为三页 A4
- `导入头像`：选择本地图片
- `调整头像`：拖动画布裁剪，使用滑块缩放，按钮旋转
- `导出 PDF`：使用本地 `vendor/html2pdf.bundle.min.js` 生成 PDF，失败时回退到打印保存
- `打印保存`：使用浏览器打印窗口另存为 PDF

简历正文可以直接点击文字修改。
